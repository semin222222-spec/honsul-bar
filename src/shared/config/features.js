// 기능 on/off 스위치 (feature flags)
//
// 현장 운영 상황에 따라 손님 앱의 일부 기능을 켜고 끌 때 사용한다.
// 코드를 지우지 않고 이 값만 바꾸면 되도록 한곳에 모아둔다.

export const features = {
  // 손님이 메뉴에서 직접 "주문하기"로 주문을 넣을 수 있는지 여부.
  //
  //   true  → 손님이 QR/폰으로 직접 주문 가능 (수량 선택 + 주문 버튼 표시)
  //   false → 메뉴는 그대로 보이지만 주문 기능은 숨김 (사장님이 직접 주문 받는 모드)
  //
  // 다시 손님 직접 주문을 켜고 싶으면 이 값을 true 로만 바꾸면 된다.
  customerOrderingEnabled: false,
};
