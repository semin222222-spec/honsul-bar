# WORKFLOW

이 프로젝트의 기본 작업 흐름은 작은 단위로 요청을 고정하고, 테스트로 먼저 실패를 확인한 뒤, 최소 구현으로 통과시키는 방식이다.

## 기본 루프

1. 요청을 한 문장으로 정리한다.
2. 변경 범위와 건드릴 파일을 확인한다.
3. 비즈니스 로직, 상태 전이, 권한, 데이터 변환이면 테스트를 먼저 작성한다.
4. 테스트가 실패하는 것을 확인한다. 이 단계가 Red다.
5. 가장 작은 구현으로 테스트를 통과시킨다. 이 단계가 Green이다.
6. `npm test`로 회귀를 확인한다.
7. 필요하면 `npm run lint`, `npm run build`까지 실행한다.
8. 문서나 작업 규칙이 바뀌었으면 README, docs, CLAUDE.md, WORKFLOW.md, GIT.md 중 관련 문서를 갱신한다.

## TDD 기준

- 테스트 코드는 프로젝트 루트의 `__tests__/`에 둔다.
- 새 테스트 파일은 `*.test.js` 이름을 사용한다.
- 구현 파일 옆에 `*.test.js`를 만들지 않는다.
- 테스트는 가능한 한 순수 함수, payload 변환, 권한 판단, 상태 전이를 직접 검증한다.
- Supabase 호출이 필요한 영역은 repository 경계를 두고 입력, 출력, fallback, 에러 처리를 분리해 테스트한다.
- 테스트가 현실적으로 어렵다면 이유와 수동 검증 방법을 완료 보고에 남긴다.

## Red-Green-Verify

Red:

- 실패해야 하는 테스트를 먼저 만든다.
- 테스트가 실제로 실패하지 않으면 요구사항을 검증하지 못한 것이다.

Green:

- 통과에 필요한 최소 코드만 바꾼다.
- 리팩터링은 테스트가 통과한 뒤 별도 단계로 한다.

Verify:

- 기본 검증은 `npm test`다.
- UI, 라우팅, 빌드 설정, import 구조가 바뀌면 `npm run build`를 추가한다.
- ESLint 규칙이나 React hook 경계가 걸릴 수 있으면 `npm run lint`를 추가한다.
- E2E는 기본 필수는 아니다. 손님 QR 진입, 주문, 관리자 수신 같은 실제 플로우가 바뀔 때만 별도로 요청하거나 판단해 실행한다.

## 안전 체크

- Supabase 권한, RLS, RPC, Storage, 키 관리는 `SUPABASE.md`를 원본으로 확인한다.
- React hook, Realtime cleanup, refetch 경계는 `docs/react-standards.md`를 원본으로 확인한다.
- 매장 데이터 변경은 항상 `store_id` 또는 `storeSlug` 스코프가 유지되는지 확인한다.
- 사용자 노출 문구는 한국어와 일본어 경험을 함께 고려한다.
