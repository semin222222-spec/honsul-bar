# GIT

이 저장소에서는 사용자가 명시적으로 요청했을 때만 커밋한다. 커밋 전에는 테스트와 기본 오류/보안 점검을 먼저 끝낸다.

## 커밋 전 필수 확인

1. 현재 브랜치를 확인한다.
2. `git status --short`로 변경 파일을 확인한다.
3. `npm test`를 한 번 실행한다.
4. `git diff --check`로 공백/패치 오류를 확인한다.
5. 변경 내용에 비밀값, `.env`, service role key, 관리자 키, 비공개 토큰이 포함되지 않았는지 확인한다.
6. 매장 데이터, 권한, RPC, Realtime 변경이 있으면 `store_id`/RLS/RPC 경계를 다시 확인한다.

E2E는 커밋 전 기본 필수 검증이 아니다. 손님 QR 진입, 주문, 관리자 실시간 수신처럼 실제 브라우저 플로우가 바뀐 경우에만 별도로 실행한다.

## 오류 점검

- `npm test`가 실패하면 커밋하지 않는다.
- lint/build 실패가 이미 확인된 상태면 커밋하지 않는다.
- 문서만 바꿨더라도 `git diff --check`는 실행한다.
- 실패한 검증이 있으면 원인과 남은 리스크를 먼저 보고한다.

## 보안 점검

- `.env`, `.env.*`, 로컬 상태 파일, 빌드 산출물은 커밋하지 않는다.
- Supabase anon key 외의 키는 브라우저 앱에 넣지 않는다.
- 관리자 화면 보호는 라우팅만으로 충분하지 않다. DB 정책과 RPC 권한을 함께 본다.
- 로그, 에러 메시지, 문서에 토큰이나 개인정보가 섞이지 않았는지 확인한다.
- Supabase 상세 보안 기준은 `SUPABASE.md`를 원본으로 한다.

## 커밋 작성

- 커밋 메시지는 변경 의도가 드러나게 쓴다.
- 서로 다른 목적의 변경은 가능한 한 분리한다.
- 사용자가 브랜치 변경을 명시하지 않으면 현재 브랜치에서 작업한다.
- `reset`, `checkout`, 강제 push 같은 파괴적 작업은 명시 요청 없이 하지 않는다.
